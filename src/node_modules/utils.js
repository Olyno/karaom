export async function setupImages() {
    const imgDefer = document.getElementsByTagName('img');
    for (const img of imgDefer) {
        if (img.getAttribute('data-src')) {
            img.setAttribute('src', img.getAttribute('data-src'));
        }
    }
}